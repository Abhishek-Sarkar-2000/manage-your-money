{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug UI (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://127.0.0.1:5000",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "Debug UI (Edge)",
      "type": "msedge",
      "request": "launch",
      "url": "http://127.0.0.1:5000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}